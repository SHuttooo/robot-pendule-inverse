# Versions avec lesquelles tout a ete entraine et verifie (Python 3.12, Windows).
mujoco==3.12.0
gymnasium==1.3.0
stable-baselines3==2.9.0
numpy==2.5.2
pillow==12.3.0

# PyTorch en version CPU : un MLP de 1 634 poids va plus vite sur CPU que sur GPU.
#   pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu

# Hors pip :
#   ffmpeg  pour film.py et les montages
#   gcc     pour 12_valider_c.py (compile politique.h et compare a PyTorch)
